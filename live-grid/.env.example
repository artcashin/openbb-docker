# Default for TESTING: EODHD's public demo websocket key (from their docs and
# the official SDK's demo code — safe to commit). Serves ONLY the demo symbols
# AAPL, MSFT, TSLA, BTC-USD, ETH-USD, EURUSD — exactly the widget's default
# watchlist — on both REST snapshots and all three websocket feeds.
#
# For real watchlists, replace with a paid EODHD key that has real-time
# (websocket) access. A free-tier key gets 403 on every websocket connect.
# The REST-only "demo" token does NOT work: the SDK's WebSocketClient rejects
# it on key-format validation.
EODHD_API_KEY=OeAFFmMliFG5orCUuwAKQ8l4WWFQ67YX
